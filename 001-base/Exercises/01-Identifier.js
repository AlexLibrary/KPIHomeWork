'use strict';

//Идентификаторы

// eslint-disable-next-line prefer-const
let name = 'Александр';
// eslint-disable-next-line no-unused-vars
const year = 1993;

const hello = (name) => {
  console.log(`Ave, ${name}`);
};

hello(name);
